// Progressive-enhancement flag — see the comment above this script's tag
// in index.html. Kept as a same-origin external file (rather than an
// inline <script>) specifically so the site's Content-Security-Policy
// can use `script-src 'self'` without needing 'unsafe-inline'.
document.documentElement.classList.add('js');
